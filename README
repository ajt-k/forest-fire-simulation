# Forest Fire Simulation

To get started, create a virtualenv, activate it, and install the requirements:

```
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

To execute the code, run:
```
python first_working_animation.py
```
